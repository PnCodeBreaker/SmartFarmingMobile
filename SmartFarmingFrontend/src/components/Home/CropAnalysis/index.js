import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const CropAnalysis = () => {
  return (
    <View>
      <Text>CropAnalysis</Text>
    </View>
  );
};

export default CropAnalysis;

const styles = StyleSheet.create({});
